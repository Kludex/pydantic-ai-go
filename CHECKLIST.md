# PydanticAI Feature Parity Checklist

This is the living source of truth for parity work. Update it whenever a feature lands, a gap is discovered, or an API decision changes.

Audited upstream baseline: `pydantic/pydantic-ai@3a3e5612786c64e19312f20e3c998553edf1353b`.

The two commits after the previous `f711f5376` runtime audit only shorten upstream test-suite and coverage hot spots. They require no Go runtime change. The prior 22-commit audit mapped stable instruction IDs, durable-operation APIs, CLI MCP/tool streaming, Z.AI behavior, AG-UI event ordering, transport-based Google routing, realtime audio iterables, and `genai-prices` 0.1.5.

Status:

- [x] Implemented and covered by public-API tests.
- [~] Partially implemented or behavior differs from upstream.
- [ ] Not implemented.
- [-] Intentionally excluded. The reason must be recorded.

## P0 - Core correctness and stable public API

### Agent loop

- [x] Typed `Agent[Deps, Output]` and `Run`.
- [x] Stable application agent identity through `WithAgentName`, static or dependency-rendered descriptions, public agent/run context access, type-safe rendering failures, output-specialization preservation, and OpenTelemetry name/description attribution with explicit telemetry-only name overrides.
- [x] Plain-text and tool-based structured output.
- [x] Native structured output for OpenAI Chat Completions and Google Gemini.
- [x] Output validators with model retries via `AddOutputValidator`.
- [x] Dynamic instructions reevaluated before every model request via `AddInstructionsFunc` and `WithRunInstructionsFunc`.
- [x] Message history input and complete/new message results.
- [x] Request, cumulative input/output/total token, per-request input, tool-call, and cost limits. Request limits stop before generation; token/cost limits support post-response enforcement or opt-in pre-request counting against the final middleware request without charging projected usage to the run.
- [x] Run, model-request, and tool-call capability middleware.
- [x] Execute independent tool calls concurrently by default while preserving model order.
- [x] Per-tool sequential execution barrier via `WithSequential()`.
- [x] Agent-wide sequential tool execution via `WithSequentialToolExecution()`.
- [x] Output-tool end strategies via `WithEndStrategy`: `graceful` default, `early`, and `exhaustive`, including retry-wins.
- [x] Run cancellation through idempotent `RunContext.Cancel`, terminal `RunCancelledError`, retained usage/history/application metadata, drained concurrent tools, completed sibling results, and resumable interrupted history.
- [x] Per-run overrides cover fixed/adaptive model selection, static and per-step settings/instructions, type-safe output specialization, output mode/tool configuration, usage limits, retry limits, history, reusable tools/toolsets, and additive capabilities with setup contributions.
- [x] Typed agent/run and untyped capability model selectors run before every logical request with step, completed-history, prior-model, model-ID, deps, and usage context.
- [x] Application model IDs resolve through ordered agent/capability resolvers, cache once per run, preserve the selection token across steps, and fail with inspectable `UnknownModelIDError`.
- [x] Model-less agents can bootstrap through agent/capability selectors or model IDs, return `ErrNoModel` when unresolved, and attribute selected models on request and outer run spans.
- [x] Optional `ModelOpener` lifecycle runs once per distinct selected model, closes in reverse selection order before toolsets, uses a non-canceled cleanup context, and propagates open/close failures across ordinary and streamed runs.
- [x] Fresh run and conversation IDs populate all generated requests/responses; explicit run IDs reject history collisions, while conversation IDs inherit from history or reset through `WithConversationID("new")`.
- [x] Static and dynamic application run metadata through `WithMetadata`, `AddMetadataFunc`, `WithRunMetadata`, and `WithRunMetadataFunc`, with ordered field merging, startup/final recomputation, prompt/current-usage context, tool/capability/manual-run visibility, typed dependency checks, detached typed/cyclic map, slice, and array values, concurrent snapshots, result access, and OpenTelemetry run attribution.
- [x] Usage includes requests, successful function-tool calls, inclusive input/output totals, cache read/write, audio, reasoning, prediction, arbitrary integer details, and optional USD cost. `genai-prices` calculates each model request automatically, preserves provider costs, exposes explicit `ModelResponse.Price()` diagnostics, contributes to tracing and cost limits, and keeps unknown cost distinct from zero.
- [x] Optional context-scoped automatic-pricing diagnostics report unavailable prices, unexpected failures, and successful-calculation warnings once per response without making pricing fatal.
- [x] `StreamedRun.Usage()` returns detached live usage, including provider token snapshots, successful tool-call counts, and best-effort current-request cost before terminal completion.

### Messages and persisted history

- [x] Implemented message subset uses upstream-compatible discriminators and validates with upstream `ModelMessagesTypeAdapter`.
- [x] Text, thinking, tool call, tool return with success/failed outcome, retry prompt, and system/user prompt parts.
- [x] Plain and metadata-tagged text plus provider-neutral `ImageURL`, `VideoURL`, `AudioURL`, `DocumentURL`, and inline `BinaryContent`, including inferred media types, stable identifiers, detached application/vendor metadata, upstream URL-safe base64 and standard JSON encoding, and safe forced downloads. Both upstream raw-string and tagged-text multimodal history shapes decode compatibly.
- [~] Pinned upstream fixtures cover basic, multimodal, complete file URL and binary content, speech, untrusted-history sanitization, video URL, interrupted, synthesized-return, request-instruction, stable instruction-part IDs, dynamic-system-prompt, structured-retry, response/part-metadata, cache-point, compaction, tool-availability, local and native typed tool-search, and arbitrary usage-detail histories; Go output validates with `ModelMessagesTypeAdapter`. `ImageURL`, `VideoURL`, `AudioURL`, `DocumentURL`, `BinaryContent`, `UploadedFile`, `SpeechPart`, validated `CachePoint` markers, and binary `FilePart` responses serialize compatibly and are defensively cloned; add upstream fixtures and provider-specific direct user-content rendering as each remaining content type lands.
- [x] Realtime speech history through dual request/response `SpeechPart` values with enforced user/assistant speakers, optional transcripts, detached retained audio, interruption offsets, provider identity/details, upstream serialization, sanitization, telemetry privacy, transcript-aware text output, normalized start/delta/end streaming, whole-transcript revisions, and consumer-safe audio chunks. `PrepareModelMessages` converts speech to request-only standard user/text parts, renders interruption markers, omits empty speech, honors explicit `ModelProfile.SupportsAudioInput`, and preserves durable history across direct, agent, wrapper, per-candidate fallback, token-counting, and compaction requests. Low-level OpenAI Chat/Responses, Anthropic, and Google adapters reject unprepared speech with `ErrUnpreparedSpeech` instead of dropping it.
- [x] Provider parameters preserve static/dynamic instruction boundaries, and each sent `ModelRequest` persists its effective joined instructions across serialization and compatible history merging.
- [x] Stable addressable instruction IDs and names across agent, capability, and identified-toolset sources through `InstructionID`, source constructors, `WithInstructionParts`, `AddInstructionPart`, `AddNamedInstructionsFunc`, `CapabilityIDProvider`, and `InstructionPartsProvider`. Parts support source qualification through toolset wrappers, static-before-dynamic cache ordering, joined rendering, request-hook replacement/removal as the source of truth, aggregate-hook compatibility, detached IDs, duplicate-source validation, legacy/unknown-namespace JSON decoding, history rendering, telemetry serialization, and per-run unaddressable names.
- [x] Legacy system prompts support static values, one-time functions, and explicit stable dynamic IDs; dynamic parts retain timestamps/IDs in serialized history and reevaluate after model selection when history resumes.
- [x] System, user, tool-return, and retry request parts preserve upstream-compatible timestamps; generated parts receive UTC timestamps without rewriting history values.
- [x] Provider-native call and return response parts with distinct non-executable types, portable tool kinds, provider identity/details, upstream serialization, and normalized streaming lifecycle.
- [~] Explicit `CachePoint` user-content markers validate five-minute or one-hour TTLs, serialize compatibly, remain detached in history, stay out of telemetry content, map to Anthropic cache controls with newest-four limiting, map to OpenAI GPT-5.6 Chat/Responses explicit breakpoints, and map through OpenRouter for Anthropic, Gemini, and OpenAI GPT-5.6 routes. OpenAI's typed cache key, legacy retention, and GPT-5.6 implicit/explicit request-wide 30-minute options are detached, conflict-checked, shared by Chat/Responses, omitted from token counting, and accompanied by cache-write usage. Anthropic adds typed automatic, instruction, message, and tool-definition caching, preserves explicit marker TTLs, reserves fixed cache slots before retaining the newest message boundaries, and reports requested retention. Remaining uploaded-file mappings, Bedrock cache points, and other provider prompt-cache placement remain.
- [~] Compaction parts preserve readable summaries, opaque IDs/details, serialization, stream lifecycle, deferred-tool visibility boundaries, and same-provider Anthropic/OpenAI Responses round trips with latest-boundary history trimming. Explicit OpenAI and Anthropic compaction capabilities/settings plus the provider-neutral `ModelCompactor` direct operation are complete. Builtin-tool return variants from remaining providers remain.
- [x] Tool availability delta parts, including the legacy `added` decode alias and upstream-compatible serialization.
- [x] Implemented requests/responses preserve timestamps, local metadata, run/conversation IDs, normalized finish reasons, provider details/URLs/names, response IDs, and lifecycle state, including deprecated `vendor_*` aliases.
- [x] Text, thinking, and function-tool-call parts preserve IDs, signatures, provider names/details, and typed tool kinds through serialization, fallback replay, keyed streaming accumulation, consumer-safe copies, and metadata-bearing normalized text/thinking deltas.
- [x] Retry prompts preserve structured validation errors and timestamps, format provider feedback consistently, and retain JSON Schema keyword, location, message, and offending input details.
- [x] Failed, denied, and interrupted tool-return outcome values, typed return tool kinds, request state, and synthesized history repair after run cancellation.
- [x] Synthesized-return metadata markers and deterministic, idempotent repair of trailing, interior, shadowed-ID, malformed-order, and empty-ID dangling calls.
- [x] Orphaned tool results are removed while plain validation feedback is preserved; consecutive requests and synthetic responses are merged with tool results hoisted before user-facing content.
- [x] Rich `ToolReturn` values separate the provider-facing return value, trailing multimodal user content, local metadata, and deferred-tool reveals while preserving provider-valid concurrent ordering.
- [x] `SanitizeMessages` safely detaches untrusted histories, strips client system prompts and optional compaction boundaries, removes compaction provenance, allowlists file URL schemes and download modes, drops uploaded-file references by default, and strips unresolved trailing local tool calls while preserving provider-native calls and explicitly resolved resumptions. It returns a detached audit report instead of logging implicitly. `UnmarshalMessages` restores nested file URL, uploaded-file, and binary values in ordinary and native tool returns so recursive map/slice sanitization cannot be bypassed through serialized history.
- [x] Stable stream part IDs and keyed/interleaved text, thinking, signature, provider-metadata, and tool-argument deltas across bundled providers and fallback replay.
- [x] Explicit `PartStartEvent`, `PartDeltaEvent`, `PartEndEvent`, `FinalResultEvent`, and metadata-bearing `FinishEvent` with typed, applicable deltas.
- [x] Concurrent-safe `RunContext.Enqueue`, `EnqueueWhenIdle`, and explicit priorities inject grouped user content, request parts, or complete messages; `asap` drains before the next request or redirects final output, `when_idle` redirects only at termination, and `EnqueuedMessagesEvent` carries stamped detached messages. Undelivered groups persist through serialized and repeated deferred pauses and resumable cancellation histories, then drain only after deferred results resolve.

### Tools and toolsets

- [x] Typed function tools with reflected JSON Schema, including reusable `Tool[Deps]` values that can be registered on agents or scoped to one run.
- [x] Dependency-aware and simple tool signatures.
- [x] Raw-schema dynamic tool registration.
- [x] Tool and argument-unmarshal retries use independent per-tool counters; output retries use a separate counter.
- [~] Reflected JSON Schema supports common structs, recursive structs with stable escaped local references, arrays, maps, descriptions, and enums; schema generation is not yet full Pydantic parity.
- [x] Provider schema transforms for implemented providers: Gemini full JSON Schema wire fields, OpenAI compatibility inference/forced rewrites including recursive roots, opt-in Anthropic strict-subset conversion, and detached OpenRouter downstream transforms for legacy Google schemas plus inline-definition Qwen, Amazon, and Meta routes.
- [x] Provider-aware strict tool mode via `WithStrict()` / `WithoutStrict()`: OpenAI infers schema compatibility, Anthropic is explicit and model-gated, and Gemini 2.5+ defaults to request-wide `VALIDATED`; each provider supports alias/proxy overrides.
- [x] Surface Anthropic's lossy strict transformation of dynamic-map schemas through `anthropic.WithSchemaWarningHandler`.
- [x] Per-tool and agent-wide per-step preparation and omission via `AddPreparedTool` and `AddToolsPrepareFunc`, applied in upstream order.
- [x] Complete Draft 2020-12 tool schemas, including prepared raw schemas and asserted formats, compile before each request and validate arguments before Go decoding or execution; failures produce persisted structured retry details.
- [x] Typed semantic argument validators run after JSON Schema validation and Go decoding but before execution for dependency-aware, simple, prepared, and raw-schema tools, with retry and terminal-failure semantics.
- [x] Agent-wide and per-run function/output retry budgets, plus per-function-tool overrides and `RunContext` retry metadata.
- [x] Per-toolset retry and timeout defaults preserve explicit per-tool overrides.
- [x] Output-tool-specific retry overrides through `OutputToolConfig.MaxRetries`, including per-run output-tool configuration.
- [x] `ToolFailedf` terminal failure results with persisted `failed` outcome and no retry-budget cost.
- [x] Reflected scalar/collection/structured tool return schemas, explicit rich-return schema overrides, detached inspection, and rejection of nested rich `ToolReturn` values.
- [x] Failed and interrupted tool returns use Anthropic error results and Gemini error responses.
- [x] Unknown or prepared-out tool calls produce corrective prompts with currently available tool names and per-name retry budgets; a deferred-but-hidden tool receives one free availability correction before later refusals charge its budget.
- [x] Per-tool deadlines via `WithToolTimeout`; cooperating cancellation becomes a retry and consumes only that tool's budget.
- [~] Tool metadata is cloned for per-step preparation and excluded from provider payloads; provider-specific options remain.
- [x] Function toolsets compose through combined, filtered, prefixed, renamed, prepared, metadata, return-schema, retry-default, and timeout-default wrappers; listing and instructions reevaluate per step, wrapped calls retain original names, and toolsets can be agent-wide or per-run.
- [x] `RequireApprovalToolset` wraps all or selected original names, and `RequireApprovalToolsetWhen` evaluates validated calls dynamically; both forward instructions and run/step/open/close lifecycle.
- [x] Stateful remote toolsets support local `ToolsetID` propagation, per-run isolation, per-step replacement, open/close lifecycle, reverse-order rollback, and lifecycle forwarding through built-in wrappers.
- [x] Deferred tools can be marked individually or through `DeferLoadingToolset`, remain unavailable until revealed, deduplicate concurrent reveals in model order, and retain visibility through serialized/resumed history.
- [x] Provider-adaptive `WithToolSearch` discovery with automatic, keyword, BM25, regex, and custom strategy selection. Local search uses detached callbacks, word-bounded relevance, undiscovered-first ranking, result limits, and independent retries. Required named strategies fail when the selected model does not advertise support.
- [x] Provider-managed tool search for OpenAI Responses and Anthropic in static, streaming, and same-provider replay paths. OpenAI uses automatic hosted search or keyword/custom client execution, preserves nullable identities without guessing ambiguous pairs, and executes same-response discovered calls. Anthropic defaults to BM25, supports required BM25/regex variants, preserves caller/error details, and retains client-executed keyword/custom search. Both keep deferred definitions stable on the wire.
- [x] Reset derived deferred-tool discovery visibility at compaction part boundaries while allowing calls generated in the compacting response to use the request-time visibility snapshot; post-boundary reveals remain visible.
- [~] `NativeToolCallPart` and `NativeToolReturnPart` are distinct non-executable response parts with upstream `builtin-tool-call`/`builtin-tool-return` serialization, provider identity, typed tool kinds, outcomes, timestamps, metadata, defensive cloning, and normalized streaming start/delta/return lifecycle. Provider-neutral `NativeTool`, `WebSearchTool`, `WebFetchTool`, `CodeExecutionTool`, `MCPServerTool`, `WithNativeTools`, `WithRunNativeTools`, `Agent.AddNativeTool`, capability registration, `AddNativeToolFunc`, and `WithRunNativeToolFunc` provide detached, duplicate-safe static or per-step dependency-driven scopes with required/optional compatibility behavior. `UploadedFile` is a detached, serializable, instrumented provider-hosted reference and code-execution files are cloned with their vendor metadata. OpenAI Responses, Anthropic, Gemini Files API, and Vertex Google Cloud Storage render matching uploaded files in user prompts and reject foreign references or invalid transport URIs before transport. OpenAI Chat, Bedrock, and xAI uploaded-file rendering plus remaining provider-specific file metadata, media-type inference, and stable computed identifiers remain.
- [x] Native-or-local function-tool preferences through serialized `NativeFallbackFor`/`unless_native` and `NativeCompanionFor`/`with_native` markers with legacy `prefer_native`/`prefer_builtin` decoding, single-tool options, lifecycle-preserving toolset wrappers, and the optional `NativeToolSupportModel` extension point. Resolution runs after model-request hooks, detaches definitions and native state, drops all local fallbacks for supported native IDs, drops unsupported native tools only when optional or backed by a fallback, clears orphan companion markers, preserves definition order, and rejects unsupported required siblings. Direct requests, agent runs, fallback candidates, model wrappers, provider-direct requests, and request telemetry use the same behavior; models without explicit support knowledge retain both paths rather than guessing.
- [~] Higher-level native-or-local pairing through generic `NativeOrLocalTool[Deps]`, `NewNativeOrLocalTool`, `NewNativeOrLocalToolset`, dynamic variants with stable identity validation, and `WithNativeRequired`. Pairs register atomically as agent/run capabilities or through `Agent.AddNativeOrLocal`, retain local toolset lifecycle, resolve per fallback candidate, evaluate dependency-aware native definitions exactly once per request, reject identity drift and optional native-only definitions, and suppress unsafe local fallbacks for native-only constraints. `NewWebSearchCapability` and `NewWebFetchCapability` add focused static configuration, accept lifecycle-aware local toolsets, require native support when no local path exists, classify search domain/use/live-access constraints and fetch use limits without silently degrading them, and allow fetch domain filters to remain locally enforceable. `NewLocalWebFetchTool` and `NewWebFetchCapabilityWithLocal` provide the built-in local strategy with detached configuration, native-filter propagation, exact redirect-aware domain controls, SSRF and cloud-metadata protection, bounded compressed/decompressed downloads, redirect-cookie and sensitive-header custody, charset decoding, HTML-to-Markdown conversion, JSON formatting, Unicode-safe content truncation, binary rich returns, and retry prompts. `NewLocalWebSearchTool` and `NewWebSearchCapabilityWithDuckDuckGo` add the default first-page DuckDuckGo strategy with bounded result counts, caller-owned clients, trusted endpoint overrides, request timeouts, bounded response bodies, normalized titles/excerpts/destination URLs, retry prompts, and a recorded real-search cassette. Typed `WebSearchFunc` and `WebFetchFunc` callbacks plus `NewDynamicWebSearchCapability` and `NewDynamicWebFetchCapability` preserve stable IDs, resolve once per request, require detached concurrent-safe results, infer native-only operation when no local path exists, and reject conditionally introduced native-only constraints unless the caller explicitly suppresses the local path with `WithNativeRequired`. `NewImageGenerationCapability`, typed `ImageGenerationFunc`, and `NewDynamicImageGenerationCapability` add static and per-request image settings, optional lifecycle-aware local toolsets, native-only defaults, stable dynamic identity, detached definitions, and explicit optional/unsupported behavior. `NewMCPServerCapability` adds atomic static provider-hosted/local-client pairing, native-only defaults, detached server credentials and allowlists, matching local allowlist filtering, lifecycle-preserving fallback, and explicit optional/unsupported behavior. `mcp.NewHTTPToolset` validates HTTP endpoints, infers legacy SSE from `/sse`, otherwise selects Streamable HTTP, clones caller clients and headers, and withholds credentials from cross-origin redirects. `mcp.NewHTTPServerCapability` derives a run-isolated local fallback from the native URL, forwards detached credentials and allowlists to both paths, preserves server identity and lifecycle, and accepts caller-owned HTTP behavior. Typed `MCPServerFunc`, `DynamicMCPServerCapabilityConfig`, and `NewDynamicMCPServerCapability` add per-request URL and credential resolution with a stable server ID, authoritative detached allowlists, once-per-request execution, and explicit unsupported behavior. Add the default image fallback-model subagent after image-only typed output is available.
- [x] Translate foreign or untagged native tool-search histories into provider-neutral function call/return turns before model requests, including return-boundary splitting and request merging. Same-provider models retain native wire identity, while OpenAI and Anthropic can replay translated discoveries alongside a currently hosted search configuration.

### Deferred execution and approval

- [x] Static and dynamic approval through `WithApprovalRequired`, `WithDynamicApproval`, and explicit `RequestToolApproval` return values, including validated pending calls and local execution after resume.
- [x] Static and dynamic external execution through typed/raw constructors, `WithExternalExecution`, `WithDynamicExternalExecution`, and explicit `RequestExternalToolExecution` values, including rich, failed, or retrying results.
- [x] Explicit `DeferredToolRequests` / `DeferredToolResults` values, detached pending results, preserved partial history, all-result validation, `WithDeferredToolResults` pause/resume, and ordered inline partial resolution.
- [x] Suspended provider responses continue automatically as one logical turn in ordinary and streamed runs, with accumulate/same-ID/fresh-replacement merge modes, early usage checks, separate generation/poll ceilings, context-aware delays, and best-effort cancellation. `Resume` and `ResumeStream` continue persisted suspended history without injecting a prompt or retaining the stale suspended snapshot. Anthropic `pause_turn` reissues immediately; OpenAI Responses background mode supports create, retrieve, streamed cursor resume, static retrieval fallback, polling configuration, and cancel.
- [x] Ordered `DeferredToolCallHandler` capability hook plus `DeferredToolHandlerFunc`, with partial resolution and unresolved-call bubbling.
- [x] Approved/denied results, validated argument overrides, and detached approval metadata through `RunContext.ToolCallApproved` and `ToolCallMetadata`.
- [x] Preserve intended deferred gaps while repairing unrelated incomplete tool-call histories on ordinary resume; completed siblings are not re-executed.
- [x] Resolved calls can defer again with a new approval/external kind and detached metadata; completed sibling results remain in history, unsent resume prompts are omitted on another pause, and the next inline handler receives the replacement request without a duplicate request event. Pending kinds survive serialization through `DeferredToolKindsMetadataKey`, preventing cross-kind result substitution.

### Streaming

- [x] Provider-optional `StreamingModel` and `Agent.RunStream`.
- [x] Normalized part lifecycle and final-result events over text, thinking, compaction, function-tool calls, provider-native calls, and complete provider-native returns, with stable IDs and interleaved-delta routing.
- [x] Non-streaming fallback replay.
- [x] OpenAI Chat Completions SSE streaming.
- [x] Anthropic SSE streaming for text, thinking, function calls, usage, errors, and cancellation.
- [x] Google Gemini SSE streaming for text, thinking, function calls with IDs, usage, errors, and cancellation.
- [x] OpenAI Responses SSE streaming for text, reasoning summaries, function arguments, usage, errors, and cancellation.
- [x] Final streamed output validation; retry requests fail clearly because `RunStream` cannot start another model round after committing output.
- [x] Typed partial text and structured output snapshots through `StreamedRun.Outputs`, with partial-validator context, retry suppression, required-field checks, and a final fully validated value.
- [x] Configurable soft-maximum partial-output debouncing through `OutputsDebounced`, including text, structured output, validator grouping, final flush, and consumer-break cleanup.
- [x] Complete Draft 2020-12 constraint and format validation for partial and final tool/native outputs, followed by custom Go decoding and semantic validators.
- [x] Streaming final-output commitment: `RunStream` locks the first matching text, native, or output-tool result. Configured end strategies still govern co-emitted tools, but retries cannot revoke the committed result.
- [x] Consumer-only stream transformation through `RunEventStreamWrapper` and `StreamEventProcessor`, including automatic streaming for `Run`.
- [x] `FunctionToolCallEvent`, `FunctionToolResultEvent`, `OutputToolCallEvent`, and `OutputToolResultEvent`, with concurrent results emitted in completion order.
- [x] `DeferredToolRequestsEvent` after individual call events and `DeferredToolResultsEvent` for each inline handler result batch, without duplicate call events.
- [x] `EnqueuedMessagesEvent` is emitted once per delivered enqueue group and participates in ordinary event capability ordering and consumer cancellation.
- [x] Stream consumer detachment preserves a detached `SuspendedRun` response/history/usage snapshot without canceling the server-side job; explicit context cancellation still performs best-effort provider cancellation. OpenAI Responses tracks response IDs and the latest consumed sequence cursor before terminal events, so detached jobs resume without replaying consumed events.
- [x] Continuation-segment deltas stream live with append indexes for fresh generations and reused indexes for provider-marked background snapshots; mid-segment metadata preserves resumable detachment snapshots and the latest OpenAI sequence cursor.

## P1 - Providers and model behavior

### Provider implementations

- [~] OpenAI Chat Completions: normalized text/thinking-compatible extensions/function calls, image URL/detail/forced-download input, MP3/WAV audio URL and inline input, remote or downloaded document input with text-file delimiters, structured output, settings, metadata, provider refusal content/deltas with inspectable content-filter responses, GPT-5.6 explicit user cache breakpoints, typed cache keys/options/retention, cache read/write usage, detached typed predicted-output requests with string or segmented content and explicit cache breakpoints, model-gated native web search options with explicit compatibility overrides, and static/streamed responses are complete. Classic Chat audio generation is outside the audited upstream adapter; normalized citations remain.
- [~] OpenAI Responses: text/reasoning/function-call/compaction streaming plus authoritative terminal snapshots, response/item IDs, encrypted reasoning and compaction round trips, latest-compaction history trimming with standing-prompt provenance, stateful context management and explicit stateless `/responses/compact` with message/custom triggers, durable history replacement, combined usage accounting, detached direct compaction through `CompactModelMessages`, pre-request `/responses/input_tokens` counting with ordered text, URL/inline-image, URL audio/document, and inline-document input, native JSON Schema output in static and streamed requests, refusal events/snapshots with inspectable content-filter responses, function namespaces, portable reasoning effort, logprob requests with static/streamed text metadata, opt-in detached raw annotations with default omission across static, streamed, and resumed background responses, message phases with profile-gated same-provider replay and explicit compatibility overrides, service tiers, status, timestamps, configurable background create/poll/retrieve/cancel/detach continuation, streaming/non-streaming server/client tool search, provider-neutral hosted web search registration with settings, static/streamed normalized lifecycle, and same-provider replay, direct uploaded image/file input, GPT-5.6 explicit user cache breakpoints, typed cache keys/options/retention with cache read/write usage, code-interpreter registration, provider-filtered uploaded files, same-provider replay, optional output inclusion, normalized logs, and streamed/static image `FilePart` values, plus portable image-generation settings, provider validation/defaults, same-provider replay, normalized call/return parts, replace-in-place streamed partial/final images, managed file-search stores with optional returned chunks, static/streamed normalization, and same-provider replay, plus provider-hosted MCP servers with URL/connector routing, detached credentials/headers/tool allowlists, static/streamed list/call lifecycles, missing streamed-result backfill, same-provider replay, and explicit rejection of unsupported provider approval requests. Other multimodal response content and builtin tools remain.
- [~] Anthropic Messages: text/thinking/function-tool/compaction streaming, direct or safely downloaded image and PDF URLs, inline image/PDF/plain-text documents, portable effort-to-budget and explicit-budget thinking configuration, service-tier mapping and response metadata, signed-thinking and readable/encrypted compaction round trips, latest-compaction trimming, explicit token-triggered compaction capabilities with summary/pause options, required beta/default context management with extension overrides, pre-request `/messages/count_tokens` counting that retains function and client-executed memory declarations while omitting unsupported server tools and advisor history, multimodal input, response IDs, stop reasons, automatic ordinary/streamed `pause_turn` continuation, provider-native deferred-definition/reveal rendering, hosted BM25/regex tool search, provider-native web search, web fetch, and code execution with model-family versions, portable settings, static/streamed normalization, caller metadata, same-provider replay, uploaded prompt/container files, and container reuse, client-executed native memory backed by a required local `memory` function tool, and provider-hosted MCP URL servers with allowlists, static/streamed call-result normalization, orphan-result handling, same-provider replay, and token-count omission. Explicit user cache points and typed automatic/instruction/message/tool-definition settings map to Anthropic ephemeral controls with validated TTLs, static-before-dynamic instruction boundaries, explicit-marker preservation, mutual-exclusion checks, token-count parity, and newest-message limiting after fixed slot reservation. The provider-native advisor validates portable model/limit/cache settings, gates eligible executor families, preserves plaintext/redacted/error results, replays only while active, reports iteration usage separately from executor totals, and supports static and streamed lifecycles. Required advisor, MCP, memory, legacy web-fetch, and uploaded-file beta headers are attached without overriding caller betas. Explicit container/version settings, adaptive thinking profiles, text citations, and other native tools remain.
- [~] Google Gemini: text/thinking/function-tool streaming, generation-aware thinking levels/budgets and thought inclusion, portable penalties/logprobs/service tiers, returned static/streamed logprob and tier metadata, thought-signature round trips, native output, multimodal input, function-call/response IDs, provider-native web-search, URL-context, and code-execution declarations, Gemini 3 function/native-tool combination gating, static/streamed grounding, URL-retrieval, and executable-code/result normalization with complete provider metadata, normalized finish reasons, candidate safety ratings, prompt-feedback block reasons/messages/ratings with inspectable content-filter responses, full JSON Schema wire fields, pre-request `:countTokens`, and Gemini 2.5+ strict defaults. `TransportGeminiAPI` and `TransportVertexAI` route Developer API, regional/global/multi-region Vertex, Express Mode, custom endpoints, OAuth/ADC, provider identity, static/streamed/count requests, and Vertex service-tier headers from transport rather than provider labels. Gemini Files API and Vertex GCS user-prompt attachments, direct or safely downloaded image/audio/document/video URLs, YouTube videos, per-file media resolution, Google video metadata, and static/streamed generated-image `FilePart` handling and same-provider inline replay are complete. Image models request image modalities and render portable aspect ratio/size settings; Vertex additionally renders output format and JPEG compression while the Gemini API omits them. Managed file search renders store names, supports legacy executable-query/grounding reconstruction and Gemini 3 explicit invocation parts, fills delayed empty returns from grounding contexts, preserves call IDs/signatures, and replays same-provider history. Code-execution file attachment, remaining native tools, image-only typed agent output, Model Armor request configuration, and advanced metadata remain.
- [x] OpenAI-compatible Chat Completions and Responses configuration through detached `ProviderConfig`: stable provider identity, base URL, optional bearer authentication, provider headers/query values, dynamic request preparation, custom HTTP clients, per-request header precedence, environment base URLs, streamed identity, and explicit strict/deferred feature switches.
- [x] Azure OpenAI and Azure AI Foundry configuration through `models/azure`: current `/v1`, serverless `.models.ai.azure.com`, and legacy deployment routes; API-key and per-request Microsoft Entra token authentication; environment defaults; API-version validation; Chat Completions and Responses models; and Azure provider attribution.
- [ ] AWS Bedrock, including Converse/legacy InvokeModel token counting and the legacy InvokeModel tool-search profile that defaults to regex and rejects explicit BM25.
- [x] Z.AI Chat Completions through `models/zai`, including `ZAI_API_KEY`, GLM 5.3 Flash, preserved `reasoning_content` across same-provider turns, static and streamed reasoning, typed `clear_thinking`, generation-specific effort translation, custom gateways, detached defaults, and normalization of `sensitive`, `model_context_window_exceeded`, and `network_error` finish reasons.
- [~] OpenRouter Chat Completions through `models/openrouter`: `provider/model` validation, API-key/custom-gateway configuration, application attribution, legacy max-token and unified reasoning translation, typed detached fallback/provider/preset/transform/reasoning/usage settings, provider-reported cost/cache-write usage, routed-provider/native-finish/server-tool/annotation metadata, static/streamed reasoning, and provider-native web-search/advisor declarations are complete. OpenRouter reasoning-detail text, summaries, encrypted signatures, stable IDs, formats, and indexes normalize and replay across static and streamed requests. Embedded HTTP-200 provider errors, transient null-completion responses, and nested provider completions are classified or normalized while retaining fallback eligibility and provider identity. Typed instruction/message/tool-definition cache settings place detached Anthropic or Gemini-compatible boundaries, preserve static-before-dynamic instruction caching, include TTL only for Anthropic, keep native tools after the cached function-tool boundary, and omit cache controls for unsupported routes. `ResolvePromptCacheRetention` reports explicit Anthropic downstream cache lifetimes through defaults and per-request settings while leaving Gemini's provider-managed retention unknown. OpenRouter applies the legacy Google schema subset with definition inlining, nullable simplification, enum/union/format rewrites, and unsupported-key removal; Qwen, Amazon, and Meta routes inline non-recursive definitions while preserving recursive schemas. Anthropic routes downgrade inferred required output-tool choice to `auto` while retaining reasoning, and reject explicit required/list forcing instead of silently losing reasoning. URL, inline, and safely force-downloaded video input use OpenRouter's `video_url` extension. Document URLs use remote file input, while audio URLs are safely downloaded and MP3/WAV data uses `input_audio`. Complete downstream profile resolution, provider-specific tool flags, and the remaining settings surface remain.
- [ ] Groq, Mistral, Cohere, Cerebras, xAI, Ollama, Hugging Face, and other upstream providers.
- [~] `ModelProfile`, `ModelProfiler`, and transparent `NewProfiledModel` provide wrapper-safe model-specific structured-output defaults and prompted templates. OpenAI Responses phase replay follows the upstream model families with an explicit compatibility override; OpenRouter now applies key downstream schema and Anthropic thinking/tool-choice traits. `NativeToolSupportModel` exposes wrapper-safe per-tool support for native/local fallback selection across current providers. Profiles also control whether standard-model preparation retains realtime speech audio or uses its transcript. Bundled strict-tool, thinking, native-tool, and other provider-setting support still relies on model-name checks and provider-local switches rather than one complete profile matrix.
- [x] Provider-neutral HTTP retries through the `retries` package and a concurrency-safe `http.RoundTripper`: bounded attempts, response validation, custom retry/wait callbacks, transient network/429/5xx classification, capped integer/date `Retry-After`, exponential fallback, context-aware waits, replayable-body enforcement, response cleanup, idle-connection forwarding, provider-client integration, and fallback-eligible exhausted response errors.
- [x] `ModelTransportError` and `NewModelTransportError` normalize bundled OpenAI-compatible, Anthropic, and Google connection, client-timeout, response-read, token-count, compaction, continuation, cancellation, stream-open, error-body, and stream-read failures with inspectable model/provider/operation/cause data. Active-context failures implement `ModelAPIError` and trigger default pre-stream fallback; caller-canceled or expired contexts retain their cause without provider-failure classification, and fallback predicates never advance after context cancellation.
- [x] Transparent `ModelWrapper` delegation, provider identity, token counting, explicit compaction, prompt-cache retention, and wrapper-aware provider compaction are complete. `FallbackModel` supports ordered models, default provider-API-error fallback, composable error/response predicates, detached diagnostics, rejected-response cost accounting, streaming-open fallback, pinned suspended continuations with rewind, combined strategy/native-history behavior, and reverse lifecycle cleanup. Shared concurrency-limited and OpenTelemetry-instrumented model wrappers preserve optional model capabilities, and model concurrency gates cover token-count operations.

### Model settings

- [x] Max tokens, temperature, top-p, seed, stop sequences, and a cooperative per-request timeout in the common settings type. `TokenCountingModel` and `CountModelTokens` provide detached direct counts with the same timeout and settings validation.
- [~] Providers only forward settings they support; compatibility is not validated by profiles. OpenAI, Anthropic, and Gemini normalize their available cache, audio, reasoning, prediction, log-probability, service-tier, and finish details.
- [x] Static or per-step `RequestTimeout` bounds synchronous requests and full stream consumption while preserving earlier parent cancellation.
- [x] `ParallelToolCalls` generation setting for OpenAI Chat/Responses and Anthropic; Gemini exposes no equivalent request setting.
- [x] Detached `ThinkingSettings` with enabled/disabled and minimal through xhigh effort levels, explicit token budgets, and thought-inclusion control. OpenAI maps levels to reasoning effort, Anthropic maps levels or explicit budgets to extended thinking, and Gemini maps by generation to thinking levels or budgets.
- [~] Portable presence/frequency penalties, logit bias, logprobs/top-logprobs, auto/default/flex/priority service tiers, and detached per-request extra headers are fieldwise merged. OpenAI Chat maps all supported fields and exposes compatible endpoint flags for remote document URLs, audio data URIs, video input, and disabled document input; Responses maps logprobs and tiers; Gemini Developer API maps penalties, logprobs, and tier values; Vertex maps default/flex/priority tiers to capacity headers; Anthropic maps compatible tiers. Static and streamed OpenAI Chat/Gemini responses preserve logprobs and service metadata, while static and streamed Responses text preserves logprobs, raw annotations, and phases. OpenAI and Anthropic support detached extra request-body fields with typed-field conflict protection. OpenAI adds detached typed predicted-output requests and rejects them on Responses before transport. Responses raw annotations are an explicit detached local setting, remain absent from provider payloads, and default to omission. OpenAI and Anthropic add validated typed prompt-cache settings and wrapper-safe durable-retention reporting; OpenRouter adds validated typed routing, reasoning, usage, downstream-aware prompt-cache settings, and provider-capability-aware retention reporting. Provider-specific typed overrides and remaining provider metadata remain.
- [x] Fieldwise settings resolve per selected model in model-default, agent, capability, and run order, with prior layers visible to each callback; bundled providers expose detached defaults through `WithDefaultSettings`.

### Outputs

- [x] Text output.
- [x] Tool output.
- [~] Native output on supported providers.
- [x] Native structured output alongside function calls obeys end strategies; plain text remains non-preemptive.
- [x] Prompted JSON output fallback for reflected structured outputs, including default/custom schema instructions, validation retries, streaming, per-run overrides, and end-strategy handling.
- [x] Multiple output alternatives through typed `UnionOutput` and `NewUnionAgent`, with stable discriminators, one provider-portable envelope across tool/native/prompted modes, raw-schema alternatives, recursive and merged-definition reference rewriting, post-validation semantic conversion, conversion retries, output validators, streaming, detached schema inspection, standalone decoding, and explicit incompatibility with per-run output specialization.
- [x] Typed `OutputFunction`/`NewOutputFunctionAgent` and `TextOutputFunction`/`NewTextOutputFunctionAgent` separate model-produced structured data or plain text from the final agent type. Conversion runs inside output-processing hooks with `RunContext`, retries, final-output validators, all applicable modes, partial streaming, custom output-tool names, concurrent-safe reusable specifications, detached schema inspection, and output-function telemetry. Binary/image output has its own open item.
- [x] `OutputModeAuto` resolves the selected model's `ModelProfile` for each step, including adaptive selectors, model-request-hook switches, transparent wrappers, direct requests, streams, and each fallback candidate. Explicit agent/run modes and templates win; profiles can supply prompted templates and require schema instructions alongside native output.
- [~] Detached binary `FilePart` output, stable identifiers, vendor metadata, compatible history serialization, telemetry, static/streamed lifecycle events, and replacement deltas are complete. `SpeechPart` adds detached retained audio, transcripts, interruption state, and normalized deltas for realtime-produced history and text output. OpenAI Responses code interpreter and image generation plus Google image models emit generated images, with Google same-provider inline replay. Ordinary provider multimodal response content, normalized citations, generated audio/video, and typed image-only output remain.
- [x] Output tool name, description, strict mode, sequential execution barrier, and independent retry configuration through `OutputToolConfig`, with per-run replacement.
- [x] Output-tool definitions can be modified, renamed, or omitted from fresh copies before each request through `AddOutputToolPrepareFunc`.
- [x] Per-run output specialization through type-safe `RunAs`, `RunPartsAs`, `RunStreamAs`, and `RunStreamPartsAs`, with validator incompatibility rejected explicitly.

## P1 - Capabilities and ecosystem

### Capability framework

- [x] Setup contributions for static instructions, model settings, and raw tools.
- [x] Run, model request, tool call, and dynamic instruction hooks.
- [x] Ordered middleware composition; first capability is outermost.
- [x] `HistoryProcessor` provides composable request-only history middleware with detached input/output snapshots and `RunInfo`; durable history replacement remains an explicit model-request hook action. `TokenHistoryTrimmer` uses the selected model's complete request count and binary-searches whole user-turn boundaries while protecting current/recent turns, preserving tool call/result pairs, and returning an inspectable error when the protected suffix cannot fit. `HistorySummarizer` replaces complete old turns with one provider-neutral assistant response, reuses that durable summary through tool loops, detaches callback values, and attributes side-request usage before primary generation.
- [x] Runs, prepared model requests, function-tool validation/execution, and output validation/processing have ordered before hooks plus reverse-ordered after/error hooks in addition to middleware wrappers. Model hooks can detach snapshots, edit stable instruction parts, modify requests, switch models, recover errors, and request budgeted retries that preserve rejected responses. Tool and output hooks transform raw or typed values, recover ordinary errors, and preserve retry/failure control flow. Run wrappers can short-circuit, transform, or recover type-checked outcomes while cancellation remains terminal.
- [x] Output validation/processing hooks cover raw structured repair, schema/decoding/semantic validation, final typed processing, wrapper and recovery composition, normal and early outputs, and streaming partial/final values.
- [x] Audited upstream user-prompt/model-request/call-tools/end node lifecycles against the plain loop. Focused request, tool validation/execution, output, run-outcome, retry, deferred, and enqueue hooks cover semantic interception; public internal-node replacement remains intentionally excluded with the graph API.
- [x] Function-tool schema validation, typed decoding, and semantic argument validation are separate from local execution, with dedicated wrappers and before/after/error hooks. Static approvals and external calls defer only after validation, wrapper-modified raw arguments are revalidated, and validated values retain their registered concrete Go type. After-validation and before/after-execution hooks can request durable approval or external execution without changing tool registration; validation-error hooks cannot defer invalid arguments.
- [x] Event-stream wrapper and per-event processor with standard capability middleware ordering.
- [x] Stable capability ordering through outermost/default/innermost tiers, type/interface or pointer-instance `Wraps`/`WrappedBy` edges, dependency requirements, cycle detection, nested-group flattening, sorted setup contributions, and independently scoped run capabilities whose requirements can use agent capabilities.
- [x] `CombineCapabilities` recursively flattens ordered groups, while embeddable `WrappedCapability` decorators preserve unoverridden child hooks, setup contributions, identity, and ordering. Individual leaves are wrapped before grouping so middleware remains explicit.
- [x] Capability-provided static/per-step model settings and adaptive model selection.
- [x] Per-run capabilities are set up once per run, contribute static instructions/settings/raw tools, participate in every middleware hook, enable event processing for `Run`, and leave agent configuration unchanged.
- [x] Deferred-call handler hook with detached requests/results, ordered composition, partial handling, and streamed lifecycle events.

### Built-in capabilities

- [~] The `mcp` package provides reusable run-isolated toolsets and caller-managed shared `Session` values for custom, Streamable HTTP, SSE, and stdio transports through the official Go SDK. Toolsets import schemas, annotations, metadata, structured/text/binary results, and server instructions with timeout and error policies. Shared sessions expose detached initialization state plus direct ping, paginated tool/prompt/resource discovery, prompt rendering, resource reads, protocol-level tool calls, and `SessionToolset` reuse across concurrent agent runs without run-owned cleanup. The standard `mcpServers` JSON loader adds deterministic prefixes, headers, stdio environment/cwd settings, and recursive environment expansion. `WithSamplingModel`, `WithSamplingHandler`, and `WithElicitationHandler` add detached server-initiated requests, model/settings/content translation, informed-input callbacks, and automatic current-protocol multi-round-trip input-required retries. `WithOAuthHandler` safely attaches the official authorization-code flow to cloned Streamable HTTP transports for shared and run-scoped sessions, with state/issuer and token-storage guidance. Higher-level local-first, native-with-local-fallback, custom-local, and dependency-aware MCP capabilities are complete. Task-extension-specific APIs remain.
- [~] Provider-neutral web search and fetch are registered statically or through per-step dependency callbacks agent-wide, per-run, or by capabilities. OpenAI Chat search-preview models render web-search context size and approximate location through `web_search_options`, reject required search on unsupported models before transport, and provide an explicit compatibility override. OpenAI Responses renders web-search context size, location, allowed domains, and live-web access with static/streamed call-return parts and same-provider replay. Gemini renders `googleSearch` and `urlContext`, gates function/native combinations by generation, and normalizes static/streamed grounding and URL-retrieval metadata. Anthropic selects model-family-aware search/fetch versions, renders portable limits and filters, preserves caller metadata, and normalizes static/streamed calls, results, errors, and replay. Gemini, Anthropic, and OpenAI Responses code execution are registered and normalized in static and streamed responses. Anthropic filters uploaded files by provider, pins container uploads to the first user turn, preserves wire subtool identity, and reuses container IDs. OpenAI renders auto containers, filters file IDs, replays container state, optionally requests outputs, and exposes logs and generated images. Portable image generation is registered with validated settings; OpenAI Responses renders defaults and overrides, validates provider dimensions, replays call identity, and normalizes static, partial, and final generated files. Google image models render portable aspect ratio and image size, Vertex output format/JPEG compression, output modalities, generated files, and same-provider replay. Provider-neutral managed file search validates detached store IDs and future xAI retrieval controls. OpenAI Responses renders vector stores, optional returned chunks, static/streamed call-return parts, and same-provider replay. Google renders Gemini store names, reconstructs legacy executable-query and grounding results, handles explicit Gemini 3 invocation parts without duplicates, fills delayed contexts, preserves signatures, and replays provider history. Anthropic native memory replaces a visible local `memory` function definition with the provider command schema while preserving ordinary validated local execution and tool-result history; missing or deferred implementations fail before transport. Provider-neutral MCP server definitions validate and detach server identity, endpoints, credentials, headers, and allowlists. OpenAI Responses renders URL and connector configurations, normalizes static and streamed list/call lifecycles, backfills omitted streamed list results, rejects unsupported approval requests, and replays same-provider call identities. Anthropic renders URL servers, authorization and enabled/disabled allowlists, attaches the required beta, omits unsupported MCP data from token counts, normalizes static/streamed calls and orphan results, and replays same-provider history. Provider-neutral advisor definitions detach optional limits and validate token/cache constraints. Anthropic maps supported executor families, settings, beta headers, static/streamed calls, plaintext/redacted/error results, active-only replay, count-token omission, and separate iteration usage. OpenRouter renders advisor model/token settings and web-search context, location, domain, and use limits after ordinary function tools. xAI MCP-server rendering and collections search, Google code-execution file attachment, X search, and image-only typed output remain.
- [x] Portable thinking configuration through common model settings, including per-run and dynamic setting layers.
- [x] Provider-neutral compaction boundaries, composable message history processors, token-aware request-only trimming through `TokenHistoryTrimmer`, durable provider-neutral summarization through `HistorySummarizer`, stateful OpenAI Responses/Anthropic compaction, OpenAI stateless message/custom triggers, detached direct compaction through `ModelCompactor` and `CompactModelMessages`, durable history replacement, model-wrapper delegation, usage-limit accounting, and dedicated `compact <model>` OpenTelemetry spans are complete.
- [~] Local tool search is available as a composable toolset; deferred capability loading remains.
- [x] Prefix, rename, filter, prepare, combine, and set-tool-metadata helpers through composable toolsets.
- [x] `ReinjectSystemPrompt` restores static, one-time, and dynamic legacy system prompts to request-only history snapshots, preserves authoritative existing prompts by default, strips untrusted prompts on demand, drops emptied requests, and leaves durable history unchanged.
- [x] Central empty-response content-filter failures plus opt-in `RaiseContentFilterError` for partial/refusal responses, including OpenAI Chat/Responses refusal snapshots and deltas, Google candidate safety ratings and prompt-feedback blocks, provider-specific reason messages, complete interoperable response bodies, detached inspection, and ordinary or streamed runs.
- [x] `IncludeToolReturnSchemas`, `WithToolReturnSchemas`, selectors, and `WithReturnSchemaIncluded` advertise detached reflected or explicit return schemas. Gemini receives native `responseJsonSchema`; OpenAI Chat/Responses, Z.AI, Azure, and Anthropic receive stable JSON in descriptions. Explicit per-tool inclusion or omission wins.
- [x] Shared in-process concurrency gates support context-aware admission, running/waiting/available metrics, optional bounded-queue backpressure, model wrappers that hold slots through stream consumption, and whole-run capability limits.

### Integrations

- [x] First-class `pydantic-evals-go` task adapters through `evals.NewTextTask` and `evals.NewTask`, including typed dependencies, multimodal and per-case run options, agent errors, explicit deferred-run rejection, usage/cost/detail metrics, run/model/provider identity, and detached application metadata.
- [~] Agent/model/tool spans and request cost attributes are present. `InstrumentedModel` adds client-kind request spans, GenAI request/response/provider/server/tool/message attributes, arbitrary first-class usage details, token/cost/time-to-first-chunk histograms, stream-lifetime spans, provider identity, and independent content/binary/request-parameter privacy controls. The outermost `Instrumentation` capability adds one run/request/tool hierarchy, configurable aggregate usage names, final or deferred output, full redacted message envelopes, latest instructions, new-message indexes, agent/run/conversation baggage, duplicate suppression, failed argument-validation spans, successful deferral metadata, application run metadata, agent names and dependency-rendered descriptions, variable-instruction diagnostics based on final prepared request instructions, run messages/schemas, and output-function spans with validated arguments, converted results, tool/function identity, privacy controls, errors, middleware nesting, and version 2 legacy naming. Richer request/tool Logfire schemas/messages, all upstream event shapes, and message-fragment caching/mutation diagnostics remain.
- [ ] Logfire guidance and examples.
- [ ] AG-UI adapter, including automatic `SanitizeMessages` handling and an assistant `TEXT_MESSAGE_START` before tool-call events owned by the same response.
- [ ] Vercel AI protocol adapter with the same untrusted-history sanitization defaults.
- [ ] A2A integration.

## P2 - Broader PydanticAI surface

- [x] `AgentRun` provides manual normalized-event progression through `StartRun`, `StartRunParts`, and `ResumeRun`, with external enqueue, idle enqueue, cancellation, live usage, deterministic cleanup, and no public graph internals.
- [x] Direct `RequestModel` and single-consumer `StreamModel` APIs without an agent loop, including detached inputs/results/live snapshots, model defaults, settings validation, restored historical instructions, provider-native history adaptation, normalized fallback streaming, per-segment timeouts and pricing, model lifecycle, and automatic suspended-response continuation.
- [ ] Embeddings API and provider implementations.
- [~] Realtime voice/audio API and providers. The shared `SpeechPart`, `SpeechPartDelta`, retained-audio, transcript-revision, interruption, standard-model handoff, serialization, telemetry, and stream accumulation contracts are complete. Session lifecycle, codecs, incremental and iterable audio/image input, transcription taps, playback events, interruption controls, OpenAI Realtime, Gemini Live, and xAI Voice transports remain.
- [ ] MCP server support.
- [ ] Agent-to-agent delegation examples and usage propagation.
- [ ] CLI and web chat entry points, including MCP configuration loading and streamed tool-call display.
- [ ] Prompt templates and format helpers.
- [ ] Durable execution integrations, including the public third-party backend contract, explicitly named durable operations for capabilities, operation serialization, and backend-specific cache identity.
- [-] Public graph API and graph-backed loop - excluded because this project intentionally uses a plain loop and capability middleware.

## Quality, documentation, and maintenance

- [x] `gofmt`, `go vet`, `golangci-lint`, tests, and per-package 100% coverage in pre-commit.
- [x] Recorded OpenAI and Anthropic traffic with credentials filtered.
- [ ] Record Google Gemini cassettes when credentials are available.
- [x] CI runs the race detector plus repeated concurrent/parallel/enqueue stress tests.
- [x] CI covers Go 1.25 and 1.26, vet, lint, tests, 100% per-package coverage, replay-only cassettes, and a clean post-test worktree.
- [x] The README starts with concise, runnable agent-and-tool setup and includes verified structured-output, streaming, conversation-history, provider-selection, and fake-model examples. Focused instruction, output, provider, HTTP-retry, MCP, capability, history-sanitization/trimming/summarization, usage-limit/token-counting, multimodal-input, and OpenTelemetry guides cover stable source-qualified prompts, output functions and unions, OpenAI-compatible/Azure setup, transport retries, shared-session lifecycle, direct protocol operations, safe untrusted-history custody, image, video, audio, and document URLs plus inline media, privacy, format compatibility, and usage attribution.
- [~] Focused instruction, output, OpenRouter/provider, MCP, capability, history, compaction, usage, multimodal, native-tool, evaluation, and OpenTelemetry guides now keep advanced setup out of the README. Add guides for deferred execution and model wrappers without turning the README into an exhaustive API dump.
- [ ] Go package documentation for every public contract.
- [ ] Compatibility policy, semantic versioning policy, and changelog.
- [ ] Benchmark loop overhead, streaming, schema reflection, and parallel tools.
- [x] Use the tagged `genai-prices` Go `v0.1.5` module release instead of a commit pseudo-version.
- [x] Audited and mapped upstream runtime changes through `3a3e5612786c64e19312f20e3c998553edf1353b` without treating unimplemented changes as complete; the advance from the prior baseline changed only CI coverage collection.
- [ ] Pin `.upstream-sync.json` to the audited upstream commit.
- [ ] After parity, add the daily `gh-aw` upstream-sync workflow described in `PLAN.md`.

## Next work

1. Complete richer OpenTelemetry request/tool Logfire schemas/messages, remaining event shapes, and message-fragment caching/mutation diagnostics; agent descriptions, variable instructions, run metadata/schemas, output-function spans, stable instruction-part serialization, and dedicated side-compaction spans are complete.
2. Extend MCP shared sessions with task-extension-specific APIs; model-backed sampling, elicitation, current-protocol multi-round-trip input retries, OAuth transport attachment, and authorization guidance are complete.
3. Extend upstream message fixtures as remaining persisted part types land.
4. Add image-only typed output and the default image fallback-model subagent, then add the remaining provider-native tools, response content, metadata, and OpenAI-compatible providers; provider-neutral video URLs with safe force-download behavior, dependency-driven native-tool factories, OpenAI Responses web search/file search/code execution/image generation/MCP servers, Gemini file search/code execution/web search/fetch/image generation, Anthropic advisor/memory/code execution/web search/fetch/MCP servers, Z.AI Chat Completions, realtime speech history, OpenAI refusal handling, Google prompt-feedback blocks, system-prompt reinjection, tool return-schema advertisement, and the `pydantic-evals-go` task adapter are complete.
